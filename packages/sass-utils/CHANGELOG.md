# @pixel-forge/sass-utils

## 0.1.0

### Minor Changes

- Initial release. Ships Sass source (not compiled CSS) as subpaths `assertion`, `color`, and `palette`. Requires Dart Sass 1.71+ for the `pkg:` importer.
