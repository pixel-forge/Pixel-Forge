# @pixel-forge/utils

## 0.2.0

### Minor Changes

- 11f1f34: Add `@pixel-forge/utils/color` with branded color strings and is/assert helpers for hex, rgb, rgba, and hsl.

## 0.1.0

### Minor Changes

- d1b50fa: Initial release. ESM-only, published as per-domain subpaths (`array`, `object`, `timing`, `types`) so consumers only pull in what they import. Requires Node >=22.12 for `require()` consumers.
