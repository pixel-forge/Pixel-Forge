---
"@pixel-forge/log": minor
---

Initial release. ESM-only console loggers per runtime: `BrowserLogger` from `@pixel-forge/log/browser-logger` and `NodeLogger` from `@pixel-forge/log/node-logger`. Shared `LogLevel` and `LogParcel` live at the package root. Requires Node >=22.12 for `require()` consumers.
